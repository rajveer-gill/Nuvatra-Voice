# Force Railway to rebuild - updated 2025-12-08 07:10
# PINNED VERSIONS: openai>=1.68.0 is required for gpt-4o-mini-tts steering (the `instructions`
# kwarg on audio.speech.create; 1.40.0 raised TypeError). Still proxy-free, so httpx stays 0.27.0.
fastapi==0.104.1
uvicorn[standard]==0.24.0
openai==1.68.0
# anthropic for the Claude voice-brain path (VOICE_LLM_MODEL=claude-*). Needs httpx>=0.25,<1
# (satisfied by the pin below) and pydantic<3 (2.9.2 below). Dormant unless a claude model is set.
anthropic==0.96.0
httpx==0.27.0
python-dotenv==1.0.0
pydantic==2.9.2
email-validator>=2.0.0
pydantic-settings==2.6.1
sentry-sdk[fastapi]>=2.0.0
python-multipart==0.0.6
websockets==12.0
aiofiles==23.2.1
twilio==9.10.9
psycopg2-binary>=2.9.10
alembic>=1.13,<2
redis>=5.0.0
PyJWT[crypto]==2.8.0
pytest==7.4.3
pytest-asyncio==0.21.1
stripe>=8.0.0
# IANA timezone database for zoneinfo. python:3.11-slim ships no system tzdata, so without
# this ZoneInfo("America/Los_Angeles") raises and business-local time silently falls back to
# UTC — which broke "today"/"tomorrow" date math on calls every evening.
tzdata>=2024.1




